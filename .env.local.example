# Copy to .env.local and replace the project id when needed.
# Load these before starting the API with its `local` Spring profile.
GOOGLE_CLOUD_PROJECT=no-pain-please-local
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
API_CORS_ALLOWED_ORIGIN=http://localhost:4200
